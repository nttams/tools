sudo apt update;
sudo apt upgrade -y;

# virtual machine
sudo apt install nload vim curl git net-tools build-essential ranger openssh-server tshark -y;

# host
sudo apt install nload vim curl git net-tools build-essential ranger openssh-server wireshark virtualbox virtualbox-ext-pack python3-pip -y;

# curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# export html from matplotlib
# pip3 install mpld3

# extension md view for microsoft edge